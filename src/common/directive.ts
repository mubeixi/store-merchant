import Vue from 'vue';

// // 注册一个全局自定义指令 `wzw-finder`唤起资源管理器
// Vue.directive('wzw-finder', {
//   // 当被绑定的元素插入到 DOM 中时……
//   bind: function (el,binding, vnode, oldVnode) {
//     // 点击唤起文件上传
//     el.addEventListener('click',function(){
//       console.log(el, binding.value)
//     })
//   }
// })
